{
  "name": "portpilot",
  "version": "0.2.0",
  "private": true,
  "workspaces": ["apps/extension"],
  "scripts": {
    "build": "npm run build -w portpilot-extension",
    "typecheck": "npm run typecheck -w portpilot-extension",
    "test": "npm test -w portpilot-extension",
    "package": "npm run package -w portpilot-extension"
  }
}
